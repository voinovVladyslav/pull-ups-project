version: "3.9"

name: "pull-ups-project"

services:
    python:
        build: .
        ports:
            - 8000:8000
        volumes:
            - ./:/app
        command: python manage.py runserver 0.0.0.0:8000
        env_file: .env
